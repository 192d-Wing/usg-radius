# Example only. Do NOT commit real secrets.
# Generate with: kubectl create secret generic usg-radius-secrets \
#   --namespace usg-radius \
#   --from-literal=RADIUS_SECRET="$(openssl rand -hex 24)" \
#   --from-literal=CLIENT_1_SECRET="$(openssl rand -hex 24)" \
#   --from-literal=ADMIN_PASSWORD="$(openssl rand -base64 32)" \
#   --dry-run=client -o yaml > k8s/secret.yaml
#
# For production use an external secret manager (External Secrets Operator,
# Sealed Secrets, Vault CSI driver, AWS/GCP/Azure secret store) instead of
# in-cluster Secrets.
apiVersion: v1
kind: Secret
metadata:
  name: usg-radius-secrets
  namespace: usg-radius
type: Opaque
stringData:
  RADIUS_SECRET: "REPLACE_ME"
  CLIENT_1_SECRET: "REPLACE_ME"
  ADMIN_PASSWORD: "REPLACE_ME"
